export type NavigationOrientation = "row" | "column" | "both";

export type NavigationCell = {
    x: number;
    y: number;
};

export type NavigationGrid = {
    width: number;
    height: number;
};
