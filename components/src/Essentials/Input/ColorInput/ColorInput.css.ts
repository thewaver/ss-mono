import { style } from "@vanilla-extract/css";

export const colorInputField = style({
    display: "flex",
    pointerEvents: "all",
    width: "100%",
    border: "none",
    padding: 0,
    background: "none",
    font: "inherit",
    color: "inherit",
    textAlign: "inherit",
});
