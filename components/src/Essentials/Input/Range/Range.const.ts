import type { RangeOrientation } from "./Range.types";

export const RANGE_DEFAULTS = {
    orientation: "horizontal" as RangeOrientation,
    min: 0,
    max: 100,
    step: 1,
    thumbSize: 16,
};
