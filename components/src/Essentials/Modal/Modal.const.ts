import type { ModalAlignment, ModalRole } from "./Modal.types";

export const MODAL_DEFAULTS = {
    transitionDurationMs: 200,
    alignment: "center" as ModalAlignment,
    role: "dialog" as ModalRole,
};
