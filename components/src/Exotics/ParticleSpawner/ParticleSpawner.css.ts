import { style } from "@vanilla-extract/css";

export const particleSpawnerRoot = style({
    position: "relative",
    width: "100%",
    height: "100%",
});

export const particleSpawnerItem = style({
    position: "absolute",
    left: 0,
    top: 0,
});
