import type { PatchBoardOrientation } from "./PatchBoard.types";

export const PATCH_BOARD_DEFAULTS = {
    orientation: "horizontal" as PatchBoardOrientation,
    socketSize: 14,
    socketReach: 28,
    stepSize: 8,
};
