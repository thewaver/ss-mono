import { style } from "@vanilla-extract/css";

export const satelliteRoot = style({
    position: "relative",
    width: "fit-content",
});

export const satelliteSubject = style({
    position: "relative",
});

export const satelliteBody = style({
    position: "absolute",
});
