import { style } from "@vanilla-extract/css";

export const formSectionRoot = style({
    display: "flex",
    width: "fit-content",
    minWidth: "0 !important",
    margin: "0 !important",
    padding: "0 !important",
    border: "none !important",
});

export const formSectionCaption = style({
    padding: "0 !important",
});
