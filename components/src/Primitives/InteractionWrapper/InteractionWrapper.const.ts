import type { InteractionSizing } from "./InteractionWrapper.types";

export const INTERACTION_WRAPPER_DEFAULTS = {
    sizing: "fit-content" as InteractionSizing,
    role: "presentation" as const,
};
